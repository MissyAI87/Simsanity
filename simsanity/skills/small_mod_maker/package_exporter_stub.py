class PackageExporter:
    def export_package(self, mod_name):
        print(f"[PackageExporter] (stub) Exporting placeholder package for {mod_name}...")
        print(f"[PackageExporter] Saved to /mods/simsanity/{mod_name.lower().replace(' ', '_')}.package")
